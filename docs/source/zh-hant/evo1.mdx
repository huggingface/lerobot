# EVO1[[evo1]]

EVO1 是一個用於機器人控制的視覺-語言-動作策略（Vision-Language-Action policy），圍繞 InternVL3 骨幹網路（backbone）與連續流匹配動作頭（continuous flow-matching action head）建構而成。此 LeRobot 整合將 EVO1 以標準策略類型（standard policy type）公開，讓它能透過一般的 LeRobot 資料集（dataset）、檢查點（checkpoint）與處理器（processor）API 進行訓練與評估（training and evaluation）。

## 模型概覽（model overview）[[model-overview]]

此策略（policy）以 InternVL3 嵌入（embeds）一或多張攝影機影像（camera images）與語言任務提示詞（language task prompt），將機器人狀態／動作向量（state/action vectors）填補（pad）到固定的最大維度（fixed maximum dimensions），並以流匹配動作頭（flow-matching action head）預測未來的動作塊（predicts future action chunks）。在推論（inference）期間，策略會取樣（samples）一個動作塊（action chunk），並在再次取樣之前，從該塊（chunk）回傳 `n_action_steps` 個動作（actions）。

### LeRobot 整合涵蓋的內容（what the LeRobot integration covers）[[what-the-lerobot-integration-covers]]

- 透過 LeRobot 的標準 `policy.type=evo1` 設定（configuration）
- 具備可選 FlashAttention 回退（fallback）的 InternVL3 影像／文字嵌入（image/text embedding）
- 針對僅動作頭（action-head-only）與 VLM 微調執行（finetuning runs）的分階段微調控制（stage-based finetuning controls）
- 連續流匹配動作預測（continuous flow-matching action prediction）
- 透過 LeRobot 策略（policy）API 進行檢查點（checkpoint）儲存／載入
- 以 `lerobot-train` 進行訓練（training），並以標準策略推論（standard policy inference）API 進行評估（evaluation）

更廣泛的 EVO1 專案可能包含額外的訓練腳本（additional training scripts）與資料集工具（dataset tooling）。本頁聚焦於 LeRobot 的機器人控制策略路徑（control policy path）。

## 安裝需求（installation requirements）[[installation-requirements]]

1. 按照[安裝指南（Installation Guide）](./installation)安裝 LeRobot。
2. 安裝 EVO1 的依賴套件（dependencies）：

   ```bash
   pip install -e ".[training,evo1]"
   ```

   若要用於 LIBERO 訓練與評估（training and evaluation），請一併安裝 LIBERO 額外套件（extra）：

   ```bash
   pip install -e ".[training,evo1,libero]"
   ```

3. 只有在 `flash-attn` wheel 與您的 Python、PyTorch、CUDA 和 GPU 堆疊（stack）相容時才安裝它。當 `flash_attn` 無法使用時，EVO1 會回退（fallback）到標準注意力機制（standard attention）。

EVO1 使用原生的 Hugging Face `transformers` InternVL 實作，因此 `policy.vlm_model_name` 必須指向一個原生轉換過的檢查點（natively converted checkpoint），例如 `OpenGVLab/InternVL3-1B-hf`（注意 `-hf` 後綴）。首次執行會下載所設定的 VLM 檢查點，之後的執行會從 Hugging Face 快取（cache）重複使用它。

## 資料需求（data requirements）[[data-requirements]]

EVO1 預期一個具備以下內容的 LeRobot 資料集（dataset）：

- 一到 `policy.max_views` 個視覺觀測（visual observations），例如 `observation.images.image`
- `observation.state`
- `action`
- 位於資料集（dataset）`task` 欄位（field）中的語言任務指令（language task instruction），或以 `policy.task_field` 設定（configured）的其他欄位

狀態與動作向量（state and action vectors）會被填補（padded）到 `policy.max_state_dim` 與 `policy.max_action_dim`。預測結果在回傳（returned）之前，會被裁切回（cropped back）資料集的動作維度（dataset action dimension）。

## 使用方式（usage）[[usage]]

若要在 LeRobot 設定（configuration）中使用 EVO1，請指定：

```python
policy.type=evo1
```

預設情況下，新的 EVO1 策略（policy）會從以下位置初始化其 VLM：

```python
policy.vlm_model_name=OpenGVLab/InternVL3-1B-hf
```

一旦有了 LeRobot 格式的 EVO1 檢查點（checkpoint），即可用以下方式載入：

```python
policy.path=your-org/your-evo1-checkpoint
```

## 訓練（training）[[training]]

### 階段 1（stage 1）[[stage-1]]

階段 1 會凍結 VLM 並訓練（train）動作頭（action head）：

```bash
lerobot-train \
  --dataset.repo_id=your_org/your_dataset \
  --policy.type=evo1 \
  --policy.training_stage=stage1 \
  --policy.vlm_model_name=OpenGVLab/InternVL3-1B-hf \
  --policy.device=cuda \
  --policy.chunk_size=50 \
  --policy.n_action_steps=50 \
  --policy.max_state_dim=24 \
  --policy.max_action_dim=24 \
  --policy.optimizer_lr=1e-5 \
  --batch_size=4 \
  --steps=5000 \
  --output_dir=./outputs/evo1_stage1
```

### 階段 2（stage 2）[[stage-2]]

階段 2 會載入階段 1 的策略，但會啟動全新的最佳化器（optimizer）與排程器（scheduler）：

```bash
lerobot-train \
  --dataset.repo_id=your_org/your_dataset \
  --policy.path=./outputs/evo1_stage1/checkpoints/005000/pretrained_model \
  --policy.training_stage=stage2 \
  --policy.vlm_model_name=OpenGVLab/InternVL3-1B-hf \
  --policy.device=cuda \
  --policy.chunk_size=50 \
  --policy.n_action_steps=50 \
  --policy.max_state_dim=24 \
  --policy.max_action_dim=24 \
  --policy.optimizer_lr=1e-5 \
  --batch_size=4 \
  --steps=80000 \
  --output_dir=./outputs/evo1_stage2
```

預設情況下，`policy.training_stage` 會重新套用該階段的微調預設值（finetuning defaults）。當從階段 1 的
檢查點（checkpoint）開始階段 2 時，這一點很重要，因為階段 1 的檢查點設定（checkpoint config）會將 VLM
微調旗標（finetuning flags）儲存為停用狀態（disabled）。除非設定
`policy.apply_training_stage_defaults=false`，否則這些階段預設值會優先於已儲存或手動提供的
`policy.finetune_*` 旗標；因此只有在您要手動控制每一個微調旗標時才設定該旗標。

### 關鍵訓練參數（key training parameters）[[key-training-parameters]]

| 參數（parameter）                             | 預設（default）             | 描述（description）                                                                             |
| --------------------------------------------- | --------------------------- | ----------------------------------------------------------------------------------------------- |
| `policy.vlm_model_name`                       | `OpenGVLab/InternVL3-1B-hf` | 原生轉換過的 InternVL3 檢查點（checkpoint）或本機模型目錄（local model directory）              |
| `policy.training_stage`                       | `stage1`                    | `stage1` 訓練動作頭（trains the action head）；`stage2` 微調 VLM 分支（finetunes VLM branches） |
| `policy.apply_training_stage_defaults`        | `true`                      | 載入檢查點（checkpoint）後重新套用階段的微調預設值（reapplies stage finetuning defaults）       |
| `policy.vlm_num_layers`                       | `14`                        | 為此策略保留的 InternVL3 語言層數量（number of InternVL3 language layers kept for the policy）  |
| `policy.vlm_dtype`                            | `bfloat16`                  | 請求的 VLM dtype                                                                                |
| `policy.use_flash_attn`                       | `true`                      | 安裝時請求 FlashAttention；否則回退（falls back）                                               |
| `policy.enable_gradient_checkpointing`        | `true`                      | 在支援的 InternVL3 模組上啟用檢查點（checkpointing）                                            |
| `policy.gradient_checkpointing_use_reentrant` | `false`                     | 支援時傳給梯度檢查點（gradient checkpointing）的 reentrant 設定（setting）                      |
| `policy.chunk_size`                           | `50`                        | 每個塊預測的未來動作數量（number of future actions predicted per chunk）                        |
| `policy.n_action_steps`                       | `50`                        | 從取樣塊中消耗的動作數量（number of actions consumed from a sampled chunk）                     |
| `policy.max_state_dim`                        | `24`                        | 狀態填補維度（state padding dimension）                                                         |
| `policy.max_action_dim`                       | `24`                        | 動作填補維度（action padding dimension）                                                        |
| `policy.postprocess_action_dim`               | `null`                      | EVO1 後處理（postprocessing）後回傳（returned）的可選動作維度（optional action dimension）      |
| `policy.binarize_gripper`                     | `false`                     | 為 LIBERO 風格評估將後處理的夾爪通道（postprocessed gripper channel）二值化（binarizes）        |
| `policy.task_field`                           | `task`                      | 用作語言提示詞（language prompt）的批次欄位（batch field）                                      |

## 推論（inference）[[inference]]

用訓練好的 EVO1 檢查點（checkpoint）試試看：

```bash
lerobot-rollout \
  --policy.path=your-org/your-evo1-checkpoint \
  --inference.type=rtc \ # 可選
  ...
```

## 結果（results）[[results]]

### LIBERO 評估（LIBERO evaluation）[[libero-evaluation]]

#### 參考結果（reference result）[[reference-result]]

> [!NOTE]
> 已發布的階段 2 檢查點（Stage-2 checkpoint）通過了乾淨下載（clean-download）與 rollout 驗證：
> [`zuoxingdong/evo1_libero`](https://huggingface.co/zuoxingdong/evo1_libero)，修訂版
> [`515921f4a2c1d3f3ad523721eafa26fdf2af315b`](https://huggingface.co/zuoxingdong/evo1_libero/commit/515921f4a2c1d3f3ad523721eafa26fdf2af315b)。
> 乾淨下載評估使用 LeRobot 修訂版
> [`e40b58a8dfa9e7b86918c374791599d070518d11`](https://github.com/huggingface/lerobot/commit/e40b58a8dfa9e7b86918c374791599d070518d11)。

在 70,000 步的單次執行階段 2 檢查點產生：

| 套件（Suite）  |  成功回合 |      回合 |     成功率 |
| -------------- | --------: | --------: | ---------: |
| LIBERO Spatial |       485 |       500 |      97.0% |
| LIBERO Object  |       496 |       500 |      99.2% |
| LIBERO Goal    |       483 |       500 |      96.6% |
| LIBERO-10      |       469 |       500 |      93.8% |
| **整體**       | **1,933** | **2,000** | **96.65%** |

這些結果使用一個訓練好的檢查點與評估種子（seed）`1000`；它們不是多重種子平均或信賴度估計。

#### 參考訓練配方（reference training recipe）[[reference-training-recipe]]

已發布的檢查點在
[`train_config.json`](https://huggingface.co/zuoxingdong/evo1_libero/blob/515921f4a2c1d3f3ad523721eafa26fdf2af315b/train_config.json)
中記錄了完整解析後的階段 2 設定。實際測量的執行使用兩張 H100 GPU，搭配兩個 DDP 程序，每個程序的批次大小為 64，因此全域批次大小為 128。兩個階段都使用相同的拓撲（topology）。基礎 VLM 來自
`OpenGVLab/InternVL3-1B-hf` 的修訂版
`014c0583a0d4bedf29fbe2dbff4f865eb998e171`。
已發布的產物並未記錄精確的 LeRobot 訓練提交（commit）或其原始依賴鎖定（dependency lock），
因此下列命令會從目前的 checkout 重現所記錄的設定與拓撲，而不是逐位元重建軟體環境。

從 LeRobot 原始碼 checkout 中，安裝鎖定的依賴套件並下載該確切的 VLM 修訂版：

```bash
uv sync --locked --extra training --extra evo1 --extra libero
VLM_DIR=$(uv run hf download OpenGVLab/InternVL3-1B-hf \
  --revision=014c0583a0d4bedf29fbe2dbff4f865eb998e171)
```

階段 1 會凍結 VLM 並訓練動作頭 5,000 步：

```bash
uv run accelerate launch --num_processes=2 -m lerobot.scripts.lerobot_train \
  --dataset.repo_id=lerobot/libero \
  --dataset.revision=a1aaacb7f6cd6ee5fb43120f673cebb0cfea7dd4 \
  --dataset.video_backend=torchcodec \
  --dataset.return_uint8=true \
  --dataset.image_transforms.enable=true \
  --dataset.use_imagenet_stats=true \
  --dataset.eval_split=0.0 \
  --policy.type=evo1 \
  --policy.training_stage=stage1 \
  --policy.apply_training_stage_defaults=true \
  --policy.vlm_model_name="${VLM_DIR}" \
  --policy.vlm_num_layers=14 \
  --policy.vlm_dtype=bfloat16 \
  --policy.device=cuda \
  --policy.use_amp=true \
  --policy.use_flash_attn=true \
  --policy.enable_gradient_checkpointing=true \
  --policy.gradient_checkpointing_use_reentrant=false \
  --policy.image_resolution='[448,448]' \
  --policy.chunk_size=50 \
  --policy.n_action_steps=50 \
  --policy.max_state_dim=24 \
  --policy.max_action_dim=24 \
  --policy.dropout=0.2 \
  --policy.optimizer_lr=1e-5 \
  --policy.optimizer_weight_decay=1e-3 \
  --policy.optimizer_grad_clip_norm=1.0 \
  --policy.scheduler_warmup_steps=1000 \
  --policy.push_to_hub=false \
  --use_policy_training_preset=true \
  --batch_size=64 \
  --steps=5000 \
  --save_checkpoint=true \
  --save_checkpoint_to_hub=false \
  --save_freq=2500 \
  --log_freq=10 \
  --env_eval_freq=0 \
  --num_workers=4 \
  --prefetch_factor=2 \
  --persistent_workers=true \
  --seed=1000 \
  --wandb.enable=false \
  --output_dir=./outputs/evo1-libero-stage1-g128-5k
```

階段 2 會載入階段 1 的策略，但會啟動全新的最佳化器與排程器。它會訓練 80,000 步；
所報告的檢查點是第 70,000 步的儲存檔：

```bash
uv run accelerate launch --num_processes=2 -m lerobot.scripts.lerobot_train \
  --dataset.repo_id=lerobot/libero \
  --dataset.revision=a1aaacb7f6cd6ee5fb43120f673cebb0cfea7dd4 \
  --dataset.video_backend=torchcodec \
  --dataset.return_uint8=true \
  --dataset.image_transforms.enable=true \
  --dataset.use_imagenet_stats=true \
  --dataset.eval_split=0.0 \
  --policy.path=./outputs/evo1-libero-stage1-g128-5k/checkpoints/005000/pretrained_model \
  --policy.training_stage=stage2 \
  --policy.apply_training_stage_defaults=true \
  --policy.vlm_model_name="${VLM_DIR}" \
  --policy.vlm_num_layers=14 \
  --policy.vlm_dtype=float32 \
  --policy.device=cuda \
  --policy.use_amp=true \
  --policy.use_flash_attn=true \
  --policy.enable_gradient_checkpointing=true \
  --policy.gradient_checkpointing_use_reentrant=false \
  --policy.image_resolution='[448,448]' \
  --policy.chunk_size=50 \
  --policy.n_action_steps=50 \
  --policy.max_state_dim=24 \
  --policy.max_action_dim=24 \
  --policy.dropout=0.2 \
  --policy.optimizer_lr=1e-5 \
  --policy.optimizer_weight_decay=1e-3 \
  --policy.optimizer_grad_clip_norm=1.0 \
  --policy.scheduler_warmup_steps=1000 \
  --policy.push_to_hub=false \
  --use_policy_training_preset=true \
  --batch_size=64 \
  --steps=80000 \
  --resume=false \
  --save_checkpoint=true \
  --save_checkpoint_to_hub=false \
  --save_freq=10000 \
  --log_freq=10 \
  --env_eval_freq=0 \
  --num_workers=4 \
  --prefetch_factor=2 \
  --persistent_workers=true \
  --seed=1000 \
  --wandb.enable=false \
  --output_dir=./outputs/evo1-libero-stage2-g128-80k
```

#### 作者格式評估設定檔（author-format evaluation profile）[[author-format-evaluation-profile]]

作者格式的 EVO1 LIBERO 設定檔使用原始的 LIBERO 攝影機特徵名稱
（`observation.images.agentview_image` 與 `observation.images.robot0_eye_in_hand_image`），每 14 個動作
重新規劃一次，並在推進模擬器之前將夾爪命令二值化。EVO1 策略後處理器
可將填補的 24D 動作裁切回 7D 的 LIBERO 動作空間，並套用該夾爪二值化。若要
在相同的每個任務一回合設定下評估作者格式的檢查點，請保留原始攝影機名稱
而非預設的 `image`/`image2` 對映，並設定 LIBERO 動作後處理旗標：

```bash
lerobot-eval \
  --policy.path=your-org/your-evo1-libero-checkpoint \
  --policy.vlm_model_name=OpenGVLab/InternVL3-1B-hf \
  --policy.device=cuda \
  --policy.use_flash_attn=true \
  --policy.n_action_steps=14 \
  --policy.postprocess_action_dim=7 \
  --policy.binarize_gripper=true \
  --env.type=libero \
  --env.task=libero_object \
  --env.camera_name_mapping="{agentview_image: agentview_image, robot0_eye_in_hand_image: robot0_eye_in_hand_image}" \
  --env.observation_height=448 \
  --env.observation_width=448 \
  --eval.batch_size=1 \
  --eval.n_episodes=1
```

#### 原生 `lerobot/libero` v3 設定檔（profile）[[native-lerobotlibero-v3-profile]]

修訂版 `a1aaacb7f6cd6ee5fb43120f673cebb0cfea7dd4` 會將攝影機特徵儲存為 `image` 與
`image2`。此範例會評估全部十個 LIBERO Object 任務，並在全新的程序中啟動每個任務：

```bash
export MUJOCO_GL=egl
export PYOPENGL_PLATFORM=egl

suite=libero_object
horizon=280
for task_id in {0..9}; do
  lerobot-eval \
    --policy.path=zuoxingdong/evo1_libero \
    --policy.pretrained_revision=515921f4a2c1d3f3ad523721eafa26fdf2af315b \
    --policy.vlm_model_name=OpenGVLab/InternVL3-1B-hf \
    --policy.device=cuda \
    --policy.use_amp=true \
    --policy.vlm_dtype=bfloat16 \
    --policy.use_flash_attn=false \
    --policy.enable_gradient_checkpointing=false \
    --policy.vlm_num_layers=14 \
    --policy.image_resolution='[448,448]' \
    --policy.max_text_length=1024 \
    --policy.chunk_size=50 \
    --policy.n_action_steps=14 \
    --policy.max_state_dim=24 \
    --policy.max_action_dim=24 \
    --policy.num_inference_timesteps=32 \
    --policy.postprocess_action_dim=7 \
    --policy.binarize_gripper=true \
    --policy.gripper_threshold=0.0 \
    --policy.gripper_below_threshold_value=-1.0 \
    --policy.gripper_above_threshold_value=1.0 \
    --env.type=libero \
    --env.task="${suite}" \
    --env.task_ids="[${task_id}]" \
    --env.camera_name=agentview_image,robot0_eye_in_hand_image \
    --env.camera_name_mapping="{agentview_image: image, robot0_eye_in_hand_image: image2}" \
    --env.control_mode=relative \
    --env.obs_type=pixels_agent_pos \
    --env.observation_width=448 \
    --env.observation_height=448 \
    --env.init_states=true \
    --env.episode_length="${horizon}" \
    --env.render_mode=rgb_array \
    --env.max_parallel_tasks=1 \
    --eval.n_episodes=50 \
    --eval.batch_size=1 \
    --eval.use_async_envs=false \
    --eval.recording=false \
    --seed=1000 \
    --output_dir="./outputs/evo1-libero-stage2-70k-eval/${suite}/task-${task_id}" \
    --job_name="evo1-libero-stage2-70k-${suite}-task-${task_id}"
done
```

針對每個套件，使用下列時間範圍（horizon）執行全部十個任務 ID：

| `env.task`       | `env.episode_length` |
| ---------------- | -------------------: |
| `libero_spatial` |                `280` |
| `libero_object`  |                `280` |
| `libero_goal`    |                `300` |
| `libero_10`      |                `520` |

為每一列設定 `suite` 與 `horizon`。這會為每個套件提供 500 個回合，總計 2,000 個回合，而
迴圈為每個任務啟動全新程序的方式，符合實際測量的 RNG 重置拓撲。

## 參考資料（references）[[references]]

- [EVO1 儲存庫](https://github.com/MINT-SJTU/Evo-1)
- [InternVL3-1B-hf](https://huggingface.co/OpenGVLab/InternVL3-1B-hf)

## 授權條款（license）[[license]]

此 LeRobot 整合遵循 LeRobot 所使用的 Apache 2.0 授權條款。已發布的檢查點與資料的授權，請查閱上游 EVO1 與 InternVL3 的模型頁面。
