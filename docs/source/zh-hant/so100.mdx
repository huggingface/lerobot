# SO-100

在以下步驟中，我們將說明如何組裝 SO-100 機器人。

## 採購零件（source the parts）

請依照這份 [README](https://github.com/TheRobotStudio/SO-ARM100/blob/main/SO100.md) 操作。它包含物料清單（bill of materials），並附有採購零件的連結，以及 3D 列印零件的說明。如果這是您第一次列印，或您沒有 3D 印表機，也會提供建議。

## 安裝 LeRobot（install LeRobot） 🤗

若要安裝 LeRobot，請依照我們的[安裝指南（Installation Guide）](./installation)操作。

除了這些說明之外，您還需要安裝 Feetech SDK：

```bash
pip install -e ".[feetech]"
```

## 設定馬達（configure the motors）

**注意：**
與 SO-101 不同，SO-100 的手臂一旦組裝完成，馬達接頭（motor connectors）就不容易存取，因此設定步驟（configuration step）必須事先完成。

### 1. 找出與每支手臂相關聯的 USB 連接埠（find the USB ports associated with each arm）

若要找出每個匯流排伺服轉接器（bus servo adapter）的連接埠（port），請執行此工具腳本：

```bash
lerobot-find-port
```

<hfoptions id="example">
<hfoption id="Mac">

範例輸出：

```
Finding all available ports for the MotorBus.
['/dev/tty.usbmodem575E0032081', '/dev/tty.usbmodem575E0031751']
Remove the USB cable from your MotorsBus and press Enter when done.

[...Disconnect corresponding leader or follower arm and press Enter...]

The port of this MotorsBus is /dev/tty.usbmodem575E0032081
Reconnect the USB cable.
```

其中找到的連接埠（found port）為：`/dev/tty.usbmodem575E0032081`，對應到您的主臂（leader）或從臂（follower）。

</hfoption>
<hfoption id="Linux">

在 Linux 上，您可能需要執行以下命令來授予 USB 連接埠（ports）的存取權：

```bash
sudo chmod 666 /dev/ttyACM0
sudo chmod 666 /dev/ttyACM1
```

範例輸出：

```
Finding all available ports for the MotorBus.
['/dev/ttyACM0', '/dev/ttyACM1']
Remove the usb cable from your MotorsBus and press Enter when done.

[...Disconnect corresponding leader or follower arm and press Enter...]

The port of this MotorsBus is /dev/ttyACM1
Reconnect the USB cable.
```

其中找到的連接埠（found port）為：`/dev/ttyACM1`，對應到您的主臂（leader）或從臂（follower）。

</hfoption>
</hfoptions>

### 2. 設定馬達 id 與鮑率

每個馬達在匯流排（bus）上都由一個唯一的 id 來識別（identified）。全新的馬達（motors）通常預設 id 為 `1`。為了讓馬達與控制器（controller）之間的通訊能正常運作，我們首先需要為每個馬達設定一個唯一且不同的 id。此外，資料在匯流排（bus）上傳輸的速度由鮑率（baudrate）決定。為了能互相通訊，控制器與所有馬達都需要設定（configured）為相同的鮑率。

為此，我們首先需要用控制器（controller）逐一連接每個馬達（motor）來設定這些值。由於我們會將這些參數（parameter）寫入馬達內部記憶體的非揮發性區段（non-volatile section, EEPROM），因此只需要做一次即可。

如果您是要重新利用來自其他機器人的馬達（motors），您可能也需要執行此步驟，因為 id 與鮑率（baudrate）很可能不相符。

#### 從臂（follower）

將電腦的 USB 線與電源供應器連接到從臂的控制板（follower arm’s controller board）。然後，使用您在上一步取得的連接埠（port），執行以下命令或執行 API 範例。您還需要搭配 `id` 參數（parameter）為您的從臂（follower arm）命名。

若需要如何設定馬達 id 的視覺化參考，請參閱[這部影片](https://huggingface.co/docs/lerobot/en/so101#setup-motors-video)，我們在其中示範了 SO101 手臂的流程。

<hfoptions id="setup_motors">
<hfoption id="Command">

```bash
lerobot-setup-motors \
    --robot.type=so100_follower \
    --robot.port=/dev/tty.usbmodem585A0076841  # <- 在此貼上上一步找到的連接埠
```

</hfoption>
<hfoption id="API example">

<!-- prettier-ignore-start -->
```python
from lerobot.robots.so_follower import SO100Follower, SO100FollowerConfig

config = SO100FollowerConfig(
    port="/dev/tty.usbmodem585A0076841",
    id="my_awesome_follower_arm",
)
follower = SO100Follower(config)
follower.setup_motors()
```
<!-- prettier-ignore-end -->

</hfoption>
</hfoptions>

您應該會看到以下指示（instruction）

```
Connect the controller board to the 'gripper' motor only and press enter.
```

依照指示，插上夾爪的馬達（gripper’s motor）。確認它是唯一連接到控制板（board）的馬達（motor），且該馬達本身尚未與任何其他馬達串接（the motor itself is not yet daisy-chained to any other motor）。當您按下 `[Enter]` 時，腳本會自動為該馬達設定 id 與鮑率（baudrate）。

<details>
<summary>疑難排解</summary>

若您在此時遇到錯誤，請檢查您的線材並確認它們已正確插好：

<ul>
  <li>電源供應器</li>
  <li>電腦與控制板（controller board）之間的 USB 線</li>
  <li>控制板（controller board）到馬達（motor）的 3-pin 線（3-pin cable）</li>
</ul>

若您使用的是 Waveshare 控制板（controller board），請確認兩個跳線（jumper）都設在 `B` 通道（`B` channel, USB）上。

</details>

接著您應該會看到以下訊息：

```
'gripper' motor id set to 6
```

隨後是下一個指示：

```
Connect the controller board to the 'wrist_roll' motor only and press enter.
```

您可以從控制板拔除 3-pin 線（3-pin cable），但另一端可以保持連接在夾爪馬達（gripper motor）上，因為它已經在正確的位置。現在，將另一條 3-pin 線插到腕部滾轉馬達（wrist roll motor）上，並將它連接到控制板（controller board）。與前一個馬達（motor）一樣，確認它是唯一連接到控制板的馬達，且該馬達本身沒有連接到任何其他馬達（the motor itself isn’t connected to any other one）。

依照指示對每個馬達重複此操作（repeat the operation）。

> [!TIP]
> 在按下 Enter 前，每一步都請檢查您的接線（cabling）。舉例來說，電源供應線可能會在您操作控制板時鬆脫。

完成後，腳本就會結束，此時馬達（motors）即可使用。您現在可以將每個馬達的 3-pin 線（3-pin cable）插到下一個馬達，並將第一個馬達（id=1 的 '肩部平移 shoulder pan'）的線連接到控制板（controller board），接著就可以將控制板固定到手臂的底座上。

#### 主臂（leader）

對主臂（leader arm）執行相同的步驟。

<hfoptions id="setup_motors">
<hfoption id="Command">
```bash
lerobot-setup-motors \
    --teleop.type=so100_leader \
    --teleop.port=/dev/tty.usbmodem575E0031751  # <- 在此貼上上一步找到的連接埠
```
</hfoption>
<hfoption id="API example">

<!-- prettier-ignore-start -->
```python
from lerobot.teleoperators.so_leader import SO100Leader, SO100LeaderConfig

config = SO100LeaderConfig(
    port="/dev/tty.usbmodem585A0076841",
    id="my_awesome_leader_arm",
)
leader = SO100Leader(config)
leader.setup_motors()
```
<!-- prettier-ignore-end -->

</hfoption>
</hfoptions>

## 逐步組裝說明（step-by-step assembly instructions）

### 移除 6 個主臂馬達的齒輪（remove the gears of the 6 leader motors）

<details>
<summary><strong>移除齒輪的影片（video removing gears）</strong></summary>

<div class="video-container">
  <video controls width="600">
    <source
      src="https://github.com/user-attachments/assets/0c95b88c-5b85-413d-ba19-aee2f864f2a7"
      type="video/mp4"
    />
  </video>
</div>

</details>

依照影片移除齒輪（gears）。您需要移除主臂馬達的齒輪（gear for the motors of the leader arm）。如此一來，您將只使用馬達的位置編碼（position encoding），並降低摩擦力，以便更輕鬆地操作主臂（leader arm）。

### 清理零件（clean parts）

移除 3D 列印零件上的所有支撐材料。最簡單的方法是使用小螺絲起子伸到支撐材料下方。

### 額外指引（additional guidance）

<details>
<summary><strong>組裝手臂的影片（video assembling arms）</strong></summary>

<div class="video-container">
  <video controls width="600">
    <source
      src="https://github.com/user-attachments/assets/488a39de-0189-4461-9de3-05b015f90cca"
      type="video/mp4"
    />
  </video>
</div>

</details>

**注意：**
這部影片提供了組裝手臂的視覺化指引，但並未說明何時或如何接線。事先插入線材會比事後接線容易許多。第一支手臂可能需要 1 小時多一點才能組裝完成，但一旦您上手後，第二支手臂可以在 1 小時內組裝完成。

---

### 第一個馬達（first motor）

**步驟 2：插入線材**

- 將兩條線插入第一個馬達。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_1.webp"
  style="height:300px;"
/>

**步驟 3：安裝到底座**

- 將第一個馬達放入底座（base）。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_2.webp"
  style="height:300px;"
/>

**步驟 4：固定馬達**

- 以 4 顆螺絲固定馬達。兩顆從下方、兩顆從上方。

**步驟 5：裝上馬達固定座**

- 滑上第一個馬達固定座（motor holder），並以兩顆螺絲（每側一顆）固定它。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_4.webp"
  style="height:300px;"
/>

**步驟 6：裝上馬達喇叭盤**

- 安裝兩個馬達喇叭盤（motor holder），以一顆螺絲固定上方的喇叭盤。裝上馬達喇叭盤時，儘量不要移動馬達的位置，尤其是我們已移除齒輪的主臂（leader arm）。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_5.webp"
  style="height:300px;"
/>

<details>
  <summary>
    <strong>裝上馬達喇叭盤的影片（video adding motor horn）</strong>
  </summary>
  <video src="https://github.com/user-attachments/assets/ef3391a4-ad05-4100-b2bd-1699bf86c969"></video>
</details>

**步驟 7：裝上肩部零件**

- 將一條線引至機器人的後方，另一條引至左方或朝向您（見照片）。
- 裝上肩部（shoulder part）零件。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_6.webp"
  style="height:300px;"
/>

**步驟 8：固定肩部**

- 以上方 4 顆螺絲與下方 4 顆螺絲鎖緊肩部（shoulder part）零件
  _（轉動肩部即可存取底部的孔）。_

---

### 第二個馬達組裝（second motor assembly）

**步驟 9：安裝馬達 2**

- 從上方滑入第二個馬達，並將馬達 1 的線連接到馬達 2。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_8.webp"
  style="height:300px;"
/>

**步驟 10：裝上肩部固定座**

- 加上肩部馬達固定座（shoulder motor holder）。
- 確保馬達 1 到馬達 2 的線走在固定座（holder）後方，而另一條線則向上引出（見照片）。
- 這個部分組裝起來可能很緊，您可以像圖片那樣使用工作檯或類似的設置，將零件推到馬達周圍。

<div style="display: flex;">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_9.webp"
    style="height:250px;"
  />
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_10.webp"
    style="height:250px;"
  />
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_12.webp"
    style="height:250px;"
  />
</div>

**步驟 11：固定馬達 2**

- 以 4 顆螺絲固定第二個馬達。

**步驟 12：裝上馬達喇叭盤**

- 將兩個馬達喇叭盤（motor horns）裝到馬達 2，同樣使用喇叭盤螺絲。

**步驟 13：裝上底座**

- 以 2 顆螺絲安裝底座（base）附件。

<img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_11.webp" style="height:300px;">

**步驟 14：裝上上臂**

- 以每側 4 顆螺絲裝上上臂（upper arm）。

<img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_13.webp" style="height:300px;">

---

### 第三個馬達組裝（third motor assembly）

**步驟 15：安裝馬達 3**

- 將馬達 2 的馬達線穿過線材固定座引至馬達 3，然後以 4 顆螺絲固定馬達 3。

**步驟 16：裝上馬達喇叭盤**

- 將兩個馬達喇叭盤（motor horns）裝到馬達 3，並再次以一顆喇叭盤螺絲固定其中一個。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_14.webp"
  style="height:300px;"
/>

**步驟 17：裝上前臂**

- 以每側 4 顆螺絲將前臂（forearm）連接到馬達 3。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_15.webp"
  style="height:300px;"
/>

---

### 第四個馬達組裝（fourth motor assembly）

**步驟 18：安裝馬達 4**

- 滑入馬達 4，接上來自馬達 3 的線，並以一顆螺絲將線固定在其固定座中。

<div style="display: flex;">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_16.webp"
    style="height:300px;"
  />
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_19.webp"
    style="height:300px;"
  />
</div>

**步驟 19：裝上馬達固定座 4**

- 安裝第四個馬達固定座（motor holder，緊配合）。確保一條線向上引出，而馬達 3 的線則向下引出（見照片）。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_17.webp"
  style="height:300px;"
/>

**步驟 20：固定馬達 4 並裝上喇叭盤**

- 以 4 顆螺絲固定馬達 4 並裝上其馬達喇叭盤（motor horns），其中一個使用喇叭盤螺絲。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_18.webp"
  style="height:300px;"
/>

---

### 腕部組裝（wrist assembly）

**步驟 21：安裝馬達 5**

- 將馬達 5 插入腕部固定座（wrist holder），並以 2 顆前方螺絲固定它。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_20.webp"
  style="height:300px;"
/>

**步驟 22：裝上腕部**

- 將馬達 4 的線連接到馬達 5。並先插入夾爪（gripper）用的另一條線。
- 以兩側 4 顆螺絲將腕部（wrist）固定到馬達 4。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_22.webp"
  style="height:300px;"
/>

**步驟 23：裝上腕部喇叭盤**

- 在腕部馬達（wrist motor）上只安裝一個馬達喇叭盤（motor horn），並以一顆喇叭盤螺絲固定它。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_23.webp"
  style="height:300px;"
/>

---

### 從臂設定（follower configuration）

**步驟 24：裝上夾爪（gripper）**

- 將夾爪裝到馬達 5。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_24.webp"
  style="height:300px;"
/>

**步驟 25：安裝夾爪馬達**

- 插入夾爪（gripper）馬達，將馬達 5 的馬達線連接到馬達 6，並以每側 3 顆螺絲固定它。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_25.webp"
  style="height:300px;"
/>

**步驟 26：裝上夾爪喇叭盤與爪部**

- 裝上馬達喇叭盤（motor horns），並再次使用喇叭盤螺絲。
- 安裝夾爪爪部（gripper claw），並以兩側 4 顆螺絲固定它。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_26.webp"
  style="height:300px;"
/>

**步驟 27：安裝控制器**

- 將馬達控制器（motor controller）裝到機器人的後方。

<div style="display: flex;">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_27.webp"
    style="height:300px;"
  />
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_28.webp"
    style="height:300px;"
  />
</div>

_組裝完成，接著進行主臂（leader arm）的組裝。_

---

### 主臂設定（leader configuration）

進行主臂設定（leader configuration）時，請執行**步驟 1 到 23**。請確認您已移除馬達的齒輪（motor gears）。

**步驟 24：裝上主臂固定座**

- 將主臂固定座（leader holder）裝到腕部（wrist），並以一顆螺絲固定它。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_29.webp"
  style="height:300px;"
/>

**步驟 25：裝上握把**

- 以 4 顆螺絲將握把（handle）裝到馬達 5。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_30.webp"
  style="height:300px;"
/>

**步驟 26：安裝夾爪馬達**

- 插入夾爪（gripper）馬達，以每側 3 顆螺絲固定它，以一顆喇叭盤螺絲裝上一個馬達喇叭盤，並接上馬達線。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_31.webp"
  style="height:300px;"
/>

**步驟 27：裝上扳機**

- 以 4 顆螺絲裝上從臂扳機（follower trigger）。

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_32.webp"
  style="height:300px;"
/>

**步驟 28：安裝控制器**

- 將馬達控制器（motor controller）裝到機器人的後方。

<div style="display: flex;">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_27.webp"
    style="height:300px;"
  />
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/so100_assembly_28.webp"
    style="height:300px;"
  />
</div>

## 校正（calibrate）

接下來，您需要校正（calibrate）您的機器人，以確保主臂（leader arm）與從臂（follower arm）在相同的物理位置（physical position）時具有相同的位置值（position values）。
校正過程（calibration process）非常重要，因為它能讓在某一台機器人上訓練（training）的神經網路（neural network），也能在另一台機器人上運作。

#### 從臂（follower）

執行以下命令或 API 範例來校正從臂（calibrate the follower arm）：

<hfoptions id="calibrate_follower">
<hfoption id="Command">

```bash
lerobot-calibrate \
    --robot.type=so100_follower \
    --robot.port=/dev/tty.usbmodem58760431551 \ # <- 您機器人的連接埠
    --robot.id=my_awesome_follower_arm # <- 為機器人取一個唯一的名稱
```

</hfoption>
<hfoption id="API example">

<!-- prettier-ignore-start -->
```python
from lerobot.robots.so_follower import SO100FollowerConfig, SO100Follower

config = SO100FollowerConfig(
    port="/dev/tty.usbmodem585A0076891",
    id="my_awesome_follower_arm",
)

follower = SO100Follower(config)
follower.connect(calibrate=False)
follower.calibrate()
follower.disconnect()
```
<!-- prettier-ignore-end -->

</hfoption>
</hfoptions>

我們統一了大多數機器人的校正（calibration）方法。因此，這支 SO100 手臂的校正步驟與 Koch 和 SO101 的步驟相同。首先，我們必須將機器人移動到每個關節（joint）都位於其活動範圍中間的位置（middle of their ranges），然後按下 `Enter`。其次，我們讓所有關節都走完其完整的活動範圍（full range of motion）。可在[這裡](https://huggingface.co/docs/lerobot/en/so101#calibration-video)找到 SO101 相同過程的影片作為參考

#### 主臂（leader）

執行相同的步驟來校正主臂（calibrate the leader arm），執行以下命令或 API 範例：

<hfoptions id="calibrate_leader">
<hfoption id="Command">

```bash
lerobot-calibrate \
    --teleop.type=so100_leader \
    --teleop.port=/dev/tty.usbmodem58760431551 \ # <- 您機器人的連接埠
    --teleop.id=my_awesome_leader_arm # <- 為機器人取一個唯一的名稱
```

</hfoption>
<hfoption id="API example">

<!-- prettier-ignore-start -->
```python
from lerobot.teleoperators.so_leader import SO100LeaderConfig, SO100Leader

config = SO100LeaderConfig(
    port="/dev/tty.usbmodem58760431551",
    id="my_awesome_leader_arm",
)

leader = SO100Leader(config)
leader.connect(calibrate=False)
leader.calibrate()
leader.disconnect()
```
<!-- prettier-ignore-end -->

</hfoption>
</hfoptions>

恭喜 🎉，您的機器人已準備就緒，可以開始自行學習任務（learn a task）。依照這份教學開始訓練（training）它：[開始使用真實世界機器人（Getting started with real-world robots）](./il_robots)

> [!TIP]
> 若您有任何問題或需要協助，請至 [Discord](https://discord.com/invite/s3KuuzsPFb) 與我們聯繫。
