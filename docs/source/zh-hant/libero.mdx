# LIBERO[[libero]]

LIBERO 是一個專為研究**終身機器人學習（lifelong robot learning）**而設計的基準測試（benchmark）——其核心理念是機器人需要隨著使用者的需求持續學習和適應，而非僅進行一次預訓練（pretrained）。它提供了一組標準化的操作任務（standardized manipulation tasks），聚焦於**知識遷移（knowledge transfer）**：即機器人將已學到的知識應用於新情境的能力。透過在 LIBERO 上進行評估（evaluating），不同演算法可以公平比較，研究人員也能在彼此的成果之上持續推進。

- 論文：[Benchmarking Knowledge Transfer for Lifelong Robot Learning](https://arxiv.org/abs/2306.03310)
- GitHub：[Lifelong-Robot-Learning/LIBERO](https://github.com/Lifelong-Robot-Learning/LIBERO)
- 專案網站：[libero-project.github.io](https://libero-project.github.io)

![LIBERO 基準測試概覽](https://libero-project.github.io/assets/img/libero/fig1.png)

## 可用任務[[available-tasks]]

LIBERO 包含**五個任務套件（task suites）**，涵蓋 **130 個任務（tasks）**，範圍從簡單的物件操作（simple object manipulations）到複雜的多步驟情境（multi-step scenarios）：

| 套件（suite）  | CLI 名稱（names） | 任務數（tasks） | 描述（description）                                                          |
| -------------- | ----------------- | --------------- | ---------------------------------------------------------------------------- |
| LIBERO-Spatial | `libero_spatial`  | 10              | 需要推理（reasoning）空間關係（spatial relations）的任務（tasks）            |
| LIBERO-Object  | `libero_object`   | 10              | 以操作不同物件（manipulating different objects）為核心的任務（tasks）        |
| LIBERO-Goal    | `libero_goal`     | 10              | 目標條件式任務（Goal-conditioned tasks），目標會隨之變化（changing targets） |
| LIBERO-90      | `libero_90`       | 90              | 來自 LIBERO-100 集合的短水平任務（short-horizon tasks）                      |
| LIBERO-Long    | `libero_10`       | 10              | 來自 LIBERO-100 集合的長水平任務（long-horizon tasks）                       |

## 安裝[[installation]]

依照 LeRobot 安裝指引（installation instructions）完成後：

```bash
pip install -e ".[libero]"
```

<Tip>
LIBERO 需要 Linux 系統（`sys_platform == 'linux'`）。LeRobot 使用 MuJoCo 進行模擬（simulation）——請在訓練（training）或評估（evaluation）前設定渲染後端（rendering backend）：

```bash
export MUJOCO_GL=egl  # 用於無顯示器的伺服器（HPC、雲端）
```

</Tip>

## 評估[[evaluation]]

### 建議方式：預設評估（default evaluation）[[default-evaluation-recommended]]

在四個標準套件（standard suites）上進行評估（evaluation，每個任務 task 10 個回合 episode）：

```bash
lerobot-eval \
  --policy.path="your-policy-id" \
  --env.type=libero \
  --env.task=libero_spatial,libero_object,libero_goal,libero_10 \
  --eval.batch_size=1 \
  --eval.n_episodes=10 \
  --env.max_parallel_tasks=1
```

### 單一套件評估（single-suite evaluation）[[single-suite-evaluation]]

在一個 LIBERO 套件（suite）上進行評估（evaluation）：

```bash
lerobot-eval \
  --policy.path="your-policy-id" \
  --env.type=libero \
  --env.task=libero_object \
  --eval.batch_size=2 \
  --eval.n_episodes=3
```

- `--env.task` 選擇套件（suite，`libero_object`、`libero_spatial` 等）。
- `--env.task_ids` 限制特定的任務索引（task indices，`[0]`、`[1,2,3]` 等）。省略則執行套件（suite）中的所有任務（tasks）。
- `--eval.batch_size` 控制同時並行執行（run in parallel）的環境（environments）數量。
- `--eval.n_episodes` 設定每個任務執行的回合數（how many episodes to run per task）。

### 多套件評估（multi-suite evaluation）[[multi-suite-evaluation]]

透過傳入以逗號分隔的清單（comma-separated list），一次在多個套件（suites）上對策略（policy）進行基準測試（benchmark）：

```bash
lerobot-eval \
  --policy.path="your-policy-id" \
  --env.type=libero \
  --env.task=libero_object,libero_spatial \
  --eval.batch_size=1 \
  --eval.n_episodes=2
```

### 控制模式（control mode）[[control-mode]]

LIBERO 支援兩種控制模式（control modes）——`relative`（預設）和 `absolute`。不同的 VLA 檢查點（checkpoint）使用不同的動作參數化（action parameterization）方式進行訓練（training），因此請確保模式（mode）與您的策略（policy）相符：

```bash
--env.control_mode=relative   # or "absolute"
```

### 重置效能（reset performance）[[reset-performance]]

預設情況下，LeRobot 會保留 LIBERO 的硬重置（hard-reset）行為。在啟用固定初始狀態（fixed initial states）後，您可以選擇改用軟重置（soft reset），以跳過每個回合重建模擬器模型（simulator model）與渲染器（renderer）的步驟：

```bash
--env.init_states=true --env.hard_reset=false
```

軟重置較快，但在環境的穩定步驟（settling steps）之後，並不會與硬重置逐位元完全相同（bit-identical），因此相機觀測（camera observations）與策略結果（policy results）可能略有差異。重現基準測試結果時，請使用硬重置。

### 策略的輸入與輸出（policy inputs and outputs）[[policy-inputs-and-outputs]]

**觀測（observations）：**

- `observation.state` — 8 維本體感覺特徵（8-dim proprioceptive features，末端執行器位置 eef position、軸角方向 axis-angle orientation、夾爪關節位置 gripper qpos）
- `observation.images.image` — 主攝影機視角（main camera view, `agentview_image`），HWC uint8
- `observation.images.image2` — 腕部攝影機視角（wrist camera view, `robot0_eye_in_hand_image`），HWC uint8

<Tip warning={true}>
  LeRobot 強制要求視覺特徵（visual features）使用 `.images.*`
  前綴（prefix）。請確保您的策略設定 （policy config）中 `input_features`
  使用相同的命名鍵（naming keys）， 且資料集的元資料鍵 （dataset metadata
  keys）也遵循此慣例（convention）。如果您的資料包含不同的鍵名（keys），
  您必須重新命名觀測值（observations）以符合策略（policy）的預期，因為命名鍵（naming
  keys） 已編碼在正規化統計層中（inside the normalization statistics layer）。
</Tip>

**動作（actions）：**

- 在 `Box(-1, 1, shape=(7,))` 連續控制（continuous control）— 6D 末端執行器增量（end-effector delta） + 1D 夾爪（gripper）

### 建議的評估回合數（recommended evaluation episodes）[[recommended-evaluation-episodes]]

為了可重現的基準測試（reproducible benchmarking），建議在所有四個標準套件（standard suites, Spatial、Object、Goal、Long）上**每個任務執行 10 個回合（10 episodes per task）**。這將產生共 400 個回合，與已發表結果所使用的協定（protocol）一致。成功率可能因評估種子（evaluation seeds）不同而有幾個百分點的變動，因此建議對 3 個種子取平均。

`lerobot-eval` 會在每個成功率旁回報 95% Wilson 區間（`pc_success_ci95`）。在 10 個回合下，某個任務得分 9/10 的區間約為 60% 到 98%；而 400 個回合的套件總計在 80% 時，區間約為 76% 到 84%。若兩個策略（policy）的區間在某個任務上重疊，則該次執行尚無法證明它們在該任務上有差異。此區間涵蓋的是抽取到哪些回合的變異，並不涵蓋策略的訓練種子（training seed），後者需要超過一次的訓練執行才能衡量。

<Tip>
  若要在相同的回合上比較兩個策略（policy），請使用相同的 `--seed`、保持
  `--env.init_states=true`，並以單一批次執行每個任務 （`--eval.batch_size`
  等於每個任務的回合數）。
</Tip>

## 訓練（training）[[training]]

### 資料集（dataset）[[dataset]]

有兩個預處理的 LIBERO 資料集（dataset）與 LeRobot 完全相容。它們包含相同的示範（demonstration）與相同的結構描述（schema），差別僅在於相機影格（camera frame）的儲存方式：

|                    | [lerobot/libero](https://huggingface.co/datasets/lerobot/libero) | [HuggingFaceVLA/libero](https://huggingface.co/datasets/HuggingFaceVLA/libero) |
| ------------------ | ---------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| 回合 / 影格 / 任務 | 1,693 / 273,465 / 40                                             | 1,693 / 273,465 / 40                                                           |
| 相機               | 2× 256×256×3                                                     | 2× 256×256×3                                                                   |
| 狀態 / 動作維度    | 8 / 7                                                            | 8 / 7                                                                          |
| 資料集格式         | v3.0                                                             | v3.0                                                                           |
| 相機編碼           | MP4 視訊                                                         | PNG（parquet 內）                                                              |
| 下載大小           | **1.9 GB**                                                       | 69.9 GB                                                                        |
| 額外相依套件       | 視訊後端（`torchcodec` 或 `pyav`）                               | 無                                                                             |

**我們推薦 [lerobot/libero](https://huggingface.co/datasets/lerobot/libero)**：**下載量縮小 37 倍**，且**載入速度相當**（每工作程序約 330 個樣本/秒）。視訊重新編碼（re-encoding）會有些許失真；若您無法安裝視訊解碼後端（video decoding backend），請改用基於影像的版本。

供參考，Physical Intelligence 發布的原始資料集（dataset）：

- [physical-intelligence/libero](https://huggingface.co/datasets/physical-intelligence/libero)

<Tip>
在回報結果時，請固定 `--dataset.revision=<commit-sha>` —— Hub 資料集可能被重新上傳，成功率只有在相同的資料修訂版本（data revision）下才具可比性。
</Tip>

### 訓練指令範例（example training command）[[example-training-command]]

在推薦的資料集上訓練 SmolVLA：

```bash
lerobot-train \
  --policy.type=smolvla \
  --policy.load_vlm_weights=true \
  --policy.push_to_hub=false \
  --dataset.repo_id=lerobot/libero \
  --dataset.video_backend=torchcodec \
  --output_dir=./outputs/libero_smolvla \
  --steps=100000 \
  --batch_size=64
```

若要在 Hub 上分享結果，請將 `--policy.push_to_hub=false` 取代為 `--policy.repo_id=${HF_USER}/libero-smolvla`。使用 `lerobot-eval` 評估儲存的檢查點（checkpoint），方式如[評估](#evaluation)一節所示。

## 重現已發表的結果（reproducing published results）[[reproducing-published-results]]

我們在 LIBERO 基準測試（benchmark）上重現（reproduce）了 Pi0.5 的結果。我們取用 Physical Intelligence 的 LIBERO 基礎模型（base model, `pi05_libero`），並以 bfloat16 精度額外微調（fine-tune） 6k 步（steps），批次大小（batch size）為 256，使用 8 張 H100 GPU 和 [HuggingFace LIBERO 資料集](https://huggingface.co/datasets/HuggingFaceVLA/libero)進行訓練（training）。

微調後的模型（finetuned model）：[lerobot/pi05_libero_finetuned](https://huggingface.co/lerobot/pi05_libero_finetuned)

### 評估指令（evaluation command）[[evaluation-command]]

```bash
lerobot-eval \
  --output_dir=./eval_logs/ \
  --env.type=libero \
  --env.task=libero_spatial,libero_object,libero_goal,libero_10 \
  --eval.batch_size=1 \
  --eval.n_episodes=10 \
  --policy.path=pi05_libero_finetuned \
  --policy.n_action_steps=10 \
  --env.max_parallel_tasks=1
```

我們設定 `n_action_steps=10`，與原始 OpenPI 實作一致。

### 結果（results）[[results]]

| 模型（model）       | LIBERO Spatial | LIBERO Object | LIBERO Goal | LIBERO 10 | 平均     |
| ------------------- | -------------- | ------------- | ----------- | --------- | -------- |
| **Pi0.5 (LeRobot)** | 97.0           | 99.0          | 98.0        | 96.0      | **97.5** |

這些結果與 Physical Intelligence 報告的[原始結果](https://github.com/Physical-Intelligence/openpi/tree/main/examples/libero#results)一致：

| 模型（model）      | LIBERO Spatial | LIBERO Object | LIBERO Goal | LIBERO 10 | 平均      |
| ------------------ | -------------- | ------------- | ----------- | --------- | --------- |
| **Pi0.5 (OpenPI)** | 98.8           | 98.2          | 98.0        | 92.4      | **96.85** |
