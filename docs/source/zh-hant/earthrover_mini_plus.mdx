# EarthRover Mini Plus[[earthrover-mini-plus]]

<img
  src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/Earth_Rover_Mini_5_240c9adc-4f9e-44b7-982f-5d1dc24af1d8.png.webp"
  alt="EarthRover Mini Plus"
  width="70%"
/>

EarthRover Mini Plus 是一款完全開源的移動機器人（mobile robot），透過 Frodobots SDK 經由雲端連線。這讓您能夠控制機器人並錄製資料集（record datasets）以訓練 AI 模型（training AI models）。

## 您需要準備的東西（what you need）[[what-you-need]]

### 硬體（hardware）[[hardware]]

- EarthRover Mini 機器人
- 具備 Python 3.12 或更新版本的電腦
- 網際網路連線

### 設定 Frodobots SDK（setting up the Frodobots SDK）[[setting-up-the-frodobots-sdk]]

機器人需要在您的電腦上執行 [Frodobots SDK](https://github.com/frodobots-org/earth-rovers-sdk)。方法如下：

1. 下載並安裝 SDK：

```bash
git clone https://github.com/frodobots-org/earth-rovers-sdk.git
cd earth-rovers-sdk
pip install -r requirements.txt
```

2. 儲存憑證（credentials）：

在您的 .env 變數（variables）中寫入 Frodobots 團隊提供的 SDK API 金鑰（key）與 bot 名稱（name）。

```bash
SDK_API_TOKEN=your_sdk_api_token_here
BOT_SLUG=your_bot_slug_here
CHROME_EXECUTABLE_PATH=/path/to/chrome_or_chromium
# 預設值為 MAP_ZOOM_LEVEL=18 https://wiki.openstreetmap.org/wiki/Zoom_levels
MAP_ZOOM_LEVEL=18
MISSION_SLUG=your_mission_slug_here
# 影像品質介於 0.1 與 1.0 之間（預設：0.8）
# 建議：0.8 以獲得更好的效能
IMAGE_QUALITY=0.8
# 影像格式：jpeg、png 或 webp（預設：png）
# 建議：jpeg 以獲得更好的效能與更低的頻寬使用量
IMAGE_FORMAT=jpeg
```

3. 啟動 SDK：

```bash
hypercorn main:app --reload
```

4. 開啟您的網頁瀏覽器並前往 `http://localhost:8000`，然後點擊「Join」

SDK 提供：

- 前後相機的即時視訊

> [!IMPORTANT]
> SDK 必須先執行，您才能使用機器人。

## 安裝 LeRobot（install LeRobot）[[install-lerobot]]

請依照我們的[安裝指南（Installation Guide）](./installation)安裝 LeRobot。

除了基本安裝之外，請安裝含硬體依賴套件（hardware dependencies）的 EarthRover Mini：

```bash
pip install -e ".[hardware]"
```

## 運作原理（how it works）[[how-it-works]]

機器人使用網際網路（internet）進行通訊：

- **移動命令（movement commands）**：透過 SDK 送出
- **相機視訊（camera video）**：從 SDK 接收
- **機器人資訊（robot info）**：來自 SDK 的電量（battery）、位置（location）、速度（speed）

您不需要插上任何東西（plug anything in），一切都透過 SDK 運作。

## 校正（calibration）[[calibration]]

不需要校正！只要 SDK 在執行中，機器人即可使用。

## 控制機器人（controlling the robot）[[controlling-the-robot]]

您可以使用鍵盤控制機器人，就像用 WASD 鍵玩電玩遊戲一樣。

### 鍵盤控制（keyboard controls）[[keyboard-controls]]

| 按鍵 | 動作（action）     |
| ---- | ------------------ |
| W    | 向前移動           |
| S    | 向後移動           |
| A    | 左轉（帶前進運動） |
| D    | 右轉（帶前進運動） |
| Q    | 原地向左旋轉       |
| E    | 原地向右旋轉       |
| X    | 停止所有移動       |
| +/=  | 提高速度           |
| -    | 降低速度           |
| ESC  | 中斷連線           |

### 速度設定（speed settings）[[speed-settings]]

您可以調整機器人的移動速度：

- **前進/後退速度（forward/backward speed）**：預設為全速（1.0）
- **轉向速度（turning speed）**：預設為全速（1.0）
- **速度變更（speed changes）**：使用 +/- 鍵每次調整 0.1

### 試試看（try it out）[[try-it-out]]

在錄製資料（recording data）前先試駕（test driving）機器人：

```python
from lerobot.robots.earthrover_mini_plus import EarthRoverMiniPlus, EarthRoverMiniPlusConfig
from lerobot.teleoperators.keyboard import KeyboardRoverTeleop, KeyboardRoverTeleopConfig

# 初始化機器人
robot_config = EarthRoverMiniPlusConfig()
robot = EarthRoverMiniPlus(robot_config)

# 初始化遙操作器
teleop_config = KeyboardRoverTeleopConfig(
    linear_speed=1.0,
    angular_speed=1.0,
    speed_increment=0.1
)
teleop = KeyboardRoverTeleop(teleop_config)

# 連線
robot.connect()
teleop.connect()

# 遙操作（使用鍵盤控制）
try:
    while True:
        action = teleop.get_action()
        robot.send_action(action)
except KeyboardInterrupt:
    pass
finally:
    robot.disconnect()
    teleop.disconnect()
```

> [!TIP]
> 若您使用 Mac，可能需要授予「終端機（Terminal）」存取鍵盤的權限以進行遙操作（teleoperation）。前往「系統偏好設定 > 安全性與隱私 > 輸入監控」，並勾選「終端機」的核取方塊。

## 錄製資料（recording data）[[recording-data]]

一旦您能好好駕駛機器人，就可以開始錄製資料來訓練 AI 模型。系統會記錄：

- **您的操作**：您如何移動機器人（前進、後退、轉向）
- **機器人所見**：
  - 兩台相機的影片
  - 機器人的速度與方向
  - 電量與位置
  - GPS 位置與訊號
  - 其他感測器資料
- **發生的時間**：所有內容的時間戳

### 設定 Hugging Face（setting up Hugging Face）[[setting-up-hugging-face]]

我們使用 Hugging Face 將您的資料儲存在線上。首先，使用您從 [Hugging Face 設定](https://huggingface.co/settings/tokens)取得的 token 登入：

```bash
hf auth login --token ${HUGGINGFACE_TOKEN} --add-to-git-credential
```

儲存您的 Hugging Face 使用者名稱：

```bash
HF_USER=$(hf auth whoami | awk -F': *' 'NR==1 {print $2}')
echo $HF_USER
```

### 開始錄製（start recording）[[start-recording]]

使用標準的錄製命令：

```bash
lerobot-record \
    --robot.type=earthrover_mini_plus \
    --teleop.type=keyboard_rover \
    --dataset.repo_id=your_username/dataset_name \
    --dataset.num_episodes=2 \
    --dataset.fps=10 \
    --dataset.single_task="Navigate around obstacles" \
    --dataset.streaming_encoding=true \
    --dataset.encoder_threads=2 \
    --display_data=true
# 選用：--dataset.rgb_encoder.vcodec=auto
```

將 `your_username/dataset_name` 替換為您的 Hugging Face 使用者名稱與資料集的名稱。

### 儲存的內容（what gets saved）[[what-gets-saved]]

您的資料集包含：

**您的動作（2 個特徵）**：

- `linear_velocity`：您向前/向後移動了多少
- `angular_velocity`：您向左/向右轉了多少

**機器人觀測（24 個特徵）**：

- 前相機影片
- 後相機影片
- 當前速度
- 電量
- 方向
- GPS（緯度、經度、訊號強度）
- 網路訊號強度
- 震動程度
- 燈光狀態（開/關）
- 加速度計（x、y、z）
- 陀螺儀（x、y、z）
- 磁力計（x、y、z）
- 車輪轉速 RPM（4 個車輪）

### 您的資料會存放在哪裡（where your data goes）[[where-your-data-goes]]

在您的電腦上：`~/.cache/huggingface/lerobot/{repo-id}`

錄製完成後，您的資料會自動上傳到您的 Hugging Face 頁面：

```bash
echo https://huggingface.co/datasets/${HF_USER}/earthrover-navigation
```

您的資料集會被標記為 `LeRobot`，方便社群探索。
