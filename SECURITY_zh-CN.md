# 安全政策

## 项目状态与理念

`lerobot` 迄今为止主要是一个研究和原型设计工具，这就是为什么部署安全直到现在都不是重点。随着 `lerobot` 继续被采用和部署到生产中，我们正更加密切关注这类问题。

幸运的是，作为一个开源项目，社区也可以通过报告和修复漏洞来帮助。我们感谢你负责任地披露发现的努力，并将尽一切努力认可你的贡献。

## 支持的版本

目前，我们将 `lerobot` 视为滚动发布。我们优先对最新版本（`main` 分支）进行安全更新。请在报告前在最新版本上复现 — 我们不会将修复向后移植到旧版本。

| 版本 | 支持 |
|------|------|
| 最新 | ✅ |
| < 最新 | ❌ |

## 报告漏洞

私下报告 — **不要为可疑的漏洞公开问题或 PR。**

要报告安全问题，请使用 GitHub 安全咨询 ["Report a Vulnerability"](https://github.com/huggingface/lerobot/security/advisories/new) 标签。这会路由到维护者，在修复前保持报告私密，并让我们可以在必要时通过 GitHub 发布 CVE。`lerobot` 团队将发送响应，指示处理你的报告的下一步。我们确认有效、范围内的报告，并将让你了解修复进展。请在任何公开披露前给我们合理的修复时间窗口。

#### Hugging Face 安全团队

由于该项目是 Hugging Face 生态系统的一部分，你可以直接向 **[security@huggingface.co](mailto:security@huggingface.co)** 提交漏洞报告。Hugging Face 安全团队的成员将审查报告并建议下一步。在初步回复你的报告后，安全团队将让你了解修复和完整公告的进展，并可能要求额外的信息或指导。

## 认可

我们不提供金钱奖励。对于有效、范围内的报告，我们在发布的 GitHub 安全咨询中感谢你，并在相关 CVE 中将你列为报告者。告诉我们你希望如何被认可（姓名或用户名）。

## 你的报告必须包含的内容

我们收到大量报告。要进入分类，报告**必须**遵循以下结构。将此块复制到你的提交中并填写每个字段。缺少版本、概念验证或影响的报告将作为不完整返回，在提供之前不会调查。

```markdown
### 摘要

一句话：漏洞是什么以及在哪里。

### 受影响版本 / 提交

你复现的确切发布版本或提交 SHA（例如 v4.57.0 / a1b2c3d）。
不是"最新"或"main"。

### 受影响组件

涉及的公共 API、模块或入口点（例如 `AutoModel.from_pretrained`）。

### 漏洞类别

类型和 CWE（如果已知）（例如 反序列化 / CWE-502，路径遍历 / CWE-22）。

### 攻击向量和前提条件

- 如何到达易受攻击的代码？（哪个 API 调用 / 输入 / 配置）
- 攻击者是谁，他们控制什么？
- 攻击工作需要什么条件？（身份验证、用户操作、非默认设置、加载恶意文件等）

### 概念验证

一个最小的、自包含的脚本或在上述版本干净安装上运行的步骤序列。包括：

- 运行的确切命令 / 代码，
- 需要的任何输入文件（附加它们，或给出一个生成它们的脚本），
- **预期**行为与你观察到的**实际**行为。
  显示函数_存在_或_可能_被滥用的片段不是 PoC。

### 影响

攻击者在真实部署中获得什么。"理论上可能…"而没有工作链不是影响。

### 范围

这跨越哪个信任边界（见下文）？如果你的发现触及"范围外"列表中的任何内容，说明哪个项目并解释为什么它仍然违反我们做出的保证。

### 建议严重性（可选）

我们分配最终严重性。仅当你有 CVSS v3.1 向量时才包括。

### 建议修复（可选）
```

> [!NOTE]
> 标准是**针对支持版本的可复现 PoC，具有跨越我们实际防御的信任边界的具体影响**（见下文范围）。理论上的报告、由扫描器或 LLM 自动生成的报告、或重述记录行为的报告将在没有详细审查的情况下关闭。

## 威胁模型和信任边界

`lerobot` 与 Hugging Face Hub 紧密耦合，用于共享数据和预训练策略。下载他人上传的工件时，你会面临风险。请阅读下面的建议，以保持你的运行时和机器人环境安全。我们_将_将破坏这些保护的任何内容视为漏洞 — 例如，尽管 `safetensors` 仅加载，代码仍执行，或固定的修订被绕过。

### 远程工件（权重和策略）

上传到 Hugging Face Hub 的模型和策略有不同的格式。我们强烈建议以 [`safetensors`](https://github.com/huggingface/safetensors) 格式上传和下载模型。`safetensors` 专门开发用于防止在你的系统上任意执行代码，这在物理硬件/机器人上运行软件时至关重要。为避免从不安全的格式（例如 `pickle`）加载模型，你应该确保优先使用 `safetensors` 文件。

### 远程代码

Hub 上的一些模型或环境可能需要 `trust_remote_code=True` 来运行自定义架构代码。使用此参数时，请务必验证建模文件的内容。我们建议在加载远程代码时设置特定的 `revision`（提交哈希），以保护自己免受存储库未经验证更新的影响。

## 范围内

我们将**已发布包代码**中的问题视为漏洞 — 库自己的 API 表面 — 攻击者可以在受害者没有选择记录的 risk 的情况下触发。例如：

- 通过正常 API 调用可到达的代码执行、内存损坏或文件访问，在**不是**用户选择加载的不可信模型/工件的输入上；
- 我们宣传的控制被绕过（例如，尽管 `safetensors` 仅加载，代码仍运行，或固定的修订被忽略）；
- 库暴露或错误处理凭据、令牌或另一个用户的数据；
- 我们记录为沙箱的后端的真实逃逸；
- 此存储库中的 CI/CD 或供应链问题。

## 范围外

以下内容**不**视为 `lerobot` 中的漏洞。如果你的发现触及这些内容之一，报告必须解释为什么它仍然违反我们做出的保证 — 否则将关闭。

- 需要加载不可信工件的问题，相当于上述记录的加载时风险（加载恶意模型、数据集、配置或 pickle 时的代码执行/文件访问）。
- `examples/`、文档、测试或其他非打包参考材料中的发现。
- 在你自己的机器上向函数提供病态输入的本地拒绝服务（高内存、慢解析、panic），没有多租户或远程服务影响。
- 模型行为：越狱、对齐失败、提示注入或有害生成。模型权重由其上传者创作；向模型所有者报告这些。
- 我们不销售的第三方依赖中的漏洞 — 向上游报告（一旦修复我们会升级）。
- 没有工作概念验证的理论问题，以及由扫描器或 LLM 自动生成但没有已验证、可复现链的报告。
- 没有证明影响的最佳实践或加固建议 — 缺少电子邮件身份验证或传输记录（MTA-STS、TLS-RPT、DMARC/SPF 调整）、缺少 HTTP 安全头、TLS 配置偏好，以及类似扫描器或配置检查器输出，没有工作利用链。

## 安全港

尊重这些指南、避免隐私侵犯和服务中断、给我们合理披露窗口的善意研究，我们不会追究。不要访问不属于你的数据，不要对 Hugging Face 生产基础设施运行测试。

<div align="center">
<sub>由 <a href="https://huggingface.co/lerobot">LeRobot</a> 团队在 <a href="https://huggingface.co">Hugging Face</a> 用 ❤️ 构建</sub>
</div>
